# FxxkMoondrop Gradle 配置
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# AndroidX
android.useAndroidX=true

# ARM64 Linux 上官方 aapt2 为 x86_64：经 qemu 包装执行（tools/qemu/aapt2-wrap.sh）
android.aapt2FromMavenOverride=/workspace/alpha_src/tools/qemu/agp-aapt2/aapt2

# 构建产物输出目录统一，便于与旧链 build2/ 并行管理
android.overridePathCheck=true

# 源码直接引用库资源（abc_ic_ab_back_material 等），恢复传统合并 R（与旧链 gen_lib_rtxt 行为一致）
android.nonTransitiveRClass=false
